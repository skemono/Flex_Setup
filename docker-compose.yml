services:
  flex:
    container_name: flex_container
    build: .
    volumes:
      # Montar directorio actual a /workspace para archivos .l
      - ./workspace:/workspace
    working_dir: /workspace
    # Mantener contenedor ejecutándose para uso interactivo
    stdin_open: true
    tty: true